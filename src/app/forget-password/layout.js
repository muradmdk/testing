export const metadata = {
    title: "Forget Password - Distrosub",
  };
  
  export default function LoginLayout({ children }) {
    return (
      <div>
        <main>{children}</main>
      </div>
    );
  }
  
