export const metadata = {
    title: "Login - Distrosub",
  };
  
  export default function LoginLayout({ children }) {
    return (
      <div>
        <main>{children}</main>
      </div>
    );
  }
  
