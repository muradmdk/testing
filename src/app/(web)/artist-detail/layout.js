export const metadata = {
    title: "Artist - Distrosub",
  };
  
  export default function ArtistDetailLayout({ children }) {
    return (
      <div>
        <main>{children}</main>
      </div>
    );
  }
  
