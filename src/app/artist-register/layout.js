
export const metadata = {
    title: "Register - Distrosub",
};
  
export default function RegisterLayout({ children }) {
    return (
        <div>
            <main>{children}</main>
        </div>
    );
}
  